[phases.setup]
nixPkgs = ['nodejs-18_x']

[phases.install]
cmds = ['npm ci --only=production']

[phases.build]
cmds = ['echo "Build complete"']

[start]
cmd = 'node app.js'

[variables]
NODE_ENV = 'production'